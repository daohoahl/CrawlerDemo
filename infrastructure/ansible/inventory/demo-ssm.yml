# Tĩnh — tên host PHẢI là Instance ID (i-...) để {{ inventory_hostname }} trong ProxyCommand đúng.
# ansible_host: 127.0.0.1 — tránh lỗi resolve hostname trên macOS.
#
# Group vars: inventory/group_vars/crawler_demo/connection.yml (SSH qua SSM Session Manager).
# Chạy:
#   ./run-site-venv.sh -i inventory/demo-ssm.yml --ask-vault-pass \
#     -e ansible_ssh_private_key_file=/Users/hungdao/Downloads/crawler_admin.pem

all:
  vars:
    ansible_ssh_private_key_file: /Users/hungdao/Downloads/crawler_admin.pem
  children:
    crawler_demo:
      hosts:
        i-09c72faf35843d437:
          ansible_host: "127.0.0.1"
