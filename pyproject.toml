[project]
name = "crawlerdemo"
version = "1.0.0"
description = "Production-ready AWS web crawler (EC2 ASG → SQS → Lambda → RDS + S3)."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
